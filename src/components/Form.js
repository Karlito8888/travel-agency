import React from 'react';
import FormTabs from '../components-sub/FormTabs';

const Form = () => {
  return (
    <div className="trip-formulaire">
      <FormTabs />
    </div>
  );
};

export default Form;